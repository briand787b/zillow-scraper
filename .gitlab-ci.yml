image: docker:latest

before_script:
  - apk update && apk add make docker-compose

build_backend:
  stage: build
  script:
    - docker-compose build backend-go

# build_chrome_app:
#   stage: build
#   script:
#     - 